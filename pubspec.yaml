name: dyplink
description: Flutter SDK for Dyplink — deep linking, attribution, in-app messages, and banners.
version: 0.1.0
homepage: https://github.com/dyplink/dyplink-flutter-sdk
repository: https://github.com/dyplink/dyplink-flutter-sdk

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.0
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0
  uuid: ^4.4.0
  app_links: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.0

flutter:
